name: Cross-platform build and release

on:
  push:
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

env:
  RELEASE_DIR: MinecraftC-1.1.2

jobs:
  linux:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v6
      - name: Install OpenGL and virtual-display dependencies
        run: sudo apt-get update && sudo apt-get install -y libgl1-mesa-dev xorg-dev xvfb libwayland-dev libxkbcommon-dev wayland-protocols extra-cmake-modules
      - name: Configure
        run: cmake -S . -B build-ci -DCMAKE_BUILD_TYPE=Release -DMINECRAFTC_FETCH_DEPENDENCIES=ON -DMINECRAFTC_PORTABLE_LAYOUT=ON -DCMAKE_INSTALL_MESSAGE=NEVER
      - name: Build
        run: cmake --build build-ci -j2
      - name: Test
        run: ctest --test-dir build-ci --output-on-failure
      - name: Install
        run: cmake --install build-ci --prefix "$RUNNER_TEMP/$RELEASE_DIR"
      - name: Verify installed assets
        run: |
          test -f "$RUNNER_TEMP/$RELEASE_DIR/assets/textures/generated/atlas.json"
          test -f "$RUNNER_TEMP/$RELEASE_DIR/assets/textures/generated/items_atlas.png"
          test -f "$RUNNER_TEMP/$RELEASE_DIR/assets/textures/generated/cloud.png"
          test "$($RUNNER_TEMP/$RELEASE_DIR/minecraftc --version)" = "MinecraftC Release-1.1.2"
      - name: Verify installed OpenGL startup
        shell: bash
        run: |
          cd "$RUNNER_TEMP"
          set +e
          timeout 8s xvfb-run -a "$RUNNER_TEMP/$RELEASE_DIR/minecraftc" > minecraftc-smoke.log 2>&1
          status=$?
          set -e
          cat minecraftc-smoke.log
          grep -q "MinecraftC initialized" minecraftc-smoke.log
          test "$status" -eq 124
      - name: Package Linux release
        run: tar -C "$RUNNER_TEMP" -czf "$RUNNER_TEMP/MinecraftC-1.1.2-linux-x86_64.tar.gz" "$RELEASE_DIR"
      - name: Upload Linux package
        uses: actions/upload-artifact@v7
        with:
          name: minecraftc-linux-x86_64
          path: ${{ runner.temp }}/MinecraftC-1.1.2-linux-x86_64.tar.gz
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

  windows:
    runs-on: windows-2025
    steps:
      - uses: actions/checkout@v6
      - name: Configure
        run: cmake -S . -B build-ci -DMINECRAFTC_FETCH_DEPENDENCIES=ON -DMINECRAFTC_PORTABLE_LAYOUT=ON -DMINECRAFTC_STATIC_MSVC_RUNTIME=ON -DCMAKE_INSTALL_MESSAGE=NEVER
      - name: Build
        run: cmake --build build-ci --config Release --parallel 2
      - name: Test
        run: ctest --test-dir build-ci -C Release --output-on-failure
      - name: Install and verify layout
        shell: pwsh
        run: |
          $stage = "$env:RUNNER_TEMP/$env:RELEASE_DIR"
          cmake --install build-ci --config Release --prefix $stage
          if (!(Test-Path "$stage/minecraftc.exe")) { throw "Executable was not installed" }
          if (!(Test-Path "$stage/assets/shaders/block.vert")) { throw "Assets were not installed" }
          if (!(Test-Path "$stage/assets/textures/generated/atlas.json")) { throw "Block atlas was not installed" }
          $version = & "$stage/minecraftc.exe" --version
          if ($version -ne "MinecraftC Release-1.1.2") { throw "Version check failed: $version" }
      - name: Package Windows release
        shell: pwsh
        run: Compress-Archive -Path "$env:RUNNER_TEMP/$env:RELEASE_DIR" -DestinationPath "$env:RUNNER_TEMP/MinecraftC-1.1.2-windows-x86_64.zip"
      - name: Upload Windows package
        uses: actions/upload-artifact@v7
        with:
          name: minecraftc-windows-x86_64
          path: ${{ runner.temp }}/MinecraftC-1.1.2-windows-x86_64.zip
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

  macos-universal:
    runs-on: macos-15
    steps:
      - uses: actions/checkout@v6
      - name: Configure universal build
        run: cmake -S . -B build-ci -DCMAKE_BUILD_TYPE=Release -DMINECRAFTC_FETCH_DEPENDENCIES=ON -DMINECRAFTC_PORTABLE_LAYOUT=ON -DCMAKE_INSTALL_MESSAGE=NEVER '-DCMAKE_OSX_ARCHITECTURES=arm64;x86_64'
      - name: Build
        run: cmake --build build-ci -j2
      - name: Test native slice
        run: ctest --test-dir build-ci --output-on-failure
      - name: Install and verify universal executable
        run: |
          cmake --install build-ci --prefix "$RUNNER_TEMP/$RELEASE_DIR"
          lipo "$RUNNER_TEMP/$RELEASE_DIR/minecraftc" -verify_arch arm64 x86_64
          test -f "$RUNNER_TEMP/$RELEASE_DIR/assets/shaders/block.vert"
          test "$($RUNNER_TEMP/$RELEASE_DIR/minecraftc --version)" = "MinecraftC Release-1.1.2"
      - name: Package macOS release
        run: tar -C "$RUNNER_TEMP" -czf "$RUNNER_TEMP/MinecraftC-1.1.2-macos-universal.tar.gz" "$RELEASE_DIR"
      - name: Upload macOS package
        uses: actions/upload-artifact@v7
        with:
          name: minecraftc-macos-universal
          path: ${{ runner.temp }}/MinecraftC-1.1.2-macos-universal.tar.gz
          if-no-files-found: error
          compression-level: 0
          retention-days: 14

  release:
    if: startsWith(github.ref, 'refs/tags/v')
    needs: [linux, windows, macos-universal]
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Download platform packages
        uses: actions/download-artifact@v8
        with:
          pattern: minecraftc-*
          path: release-assets
          merge-multiple: true
      - name: Publish GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          cd release-assets
          sha256sum MinecraftC-* > SHA256SUMS
          if gh release view "$GITHUB_REF_NAME" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" * --repo "$GITHUB_REPOSITORY" --clobber
          else
            gh release create "$GITHUB_REF_NAME" * --repo "$GITHUB_REPOSITORY" --verify-tag --generate-notes --title "MinecraftC $GITHUB_REF_NAME"
          fi
